# Particles

This example renders a desktop particle scene through `flutter_wgpu_texture`.

Run from this directory:

```bash
flutter run -d macos
```

Use the controls to adjust particle size and motion scale.
