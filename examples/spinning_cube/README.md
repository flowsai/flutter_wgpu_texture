# Spinning Cube

This example renders a rotating yellow 3D cube on a blue background using
`flutter_wgpu_texture`.

Run from this directory:

```bash
flutter run -d linux
```

The same app should work on macOS and Windows once the native desktop toolchain
is available.
