# Shader Playground

This example renders a fullscreen procedural WGSL shader through
`flutter_wgpu_texture`.

Run from this directory:

```bash
flutter run -d macos
```

Use the controls to adjust speed, noise scale, and pointer distortion. Drag or
hover over the viewport to push the flow field.
