name: flutter_wgpu_texture_custom_scene
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_wgpu_texture:
    path: ../..
  hooks: ^1.0.2
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
